# wcauto - 微信自动化工具依赖管理
# ===========================================

# 核心自动化控制依赖
pyautogui>=0.9.53,<1.0.0  # 鼠标键盘自动化控制，支持Windows GUI操作

# 剪贴板操作依赖
pyperclip>=1.8.2,<2.0.0   # 跨平台剪贴板操作，用于文本传输

# Windows UI自动化依赖
uiautomation>=2.0.15,<3.0.0  # Windows UI自动化框架，用于窗口控制和元素定位

# 系统进程管理依赖
psutil>=5.8.0,<6.0.0      # 进程管理和系统资源监控

# Windows API接口依赖
pywin32>=300,<310         # Windows API接口，用于系统级操作

# 可选依赖（开发环境使用）
# pytest>=6.0.0,<7.0.0    # 单元测试框架（开发依赖）
# black>=21.0.0,<22.0.0    # 代码格式化工具（开发依赖）
# flake8>=4.0.0,<5.0.0     # 代码质量检查（开发依赖）

# 环境要求说明
# - Python版本: 3.8及以上
# - 操作系统: Windows 10/11 (64位)
# - 微信版本: 微信桌面版 (已登录)

# 安装说明
# pip install -r requirements.txt
# 或使用 pip install -e . 进行开发模式安装